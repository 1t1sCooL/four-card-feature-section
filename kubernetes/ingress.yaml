apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: four-card-feature-section
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: mmalabugin.ru
      http:
        paths:
          - path: /FourCardFeatureSection
            pathType: Prefix
            backend:
              service:
                name: four-card-feature-section
                port:
                  number: 80
  tls:
    - hosts:
        - mmalabugin.ru
      secretName: main-page-tls
